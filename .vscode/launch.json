{
  "version": "0.2.0",
  "configurations": [
        {
      "name": "npm run build && ng run test",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/node_modules/protractor/bin/protractor",
      "cwd": "${workspaceRoot}",
      "protocol": "inspector",
      "args": [
        "${workspaceFolder}/typeScript/config/config.js"
        ] 
    }
  ]
}